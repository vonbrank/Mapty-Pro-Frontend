import { Button, styled } from "@mui/material";
import React from "react";

const MaptyProButton = styled(Button)((theme) => ({
  textTransform: "capitalize",
  fontSize: "1.6rem",
}));

export { MaptyProButton };
